@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Result</title>
    <!-- Add the Bootstrap CSS link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-size: 14px;
            color: #333;
            text-align: left;
            margin-bottom: 20px;
        }

        .result-section {
            font-size: 16px;
            color: #555;
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container mt-2">
        <h2>Task 2.1</h2>
        <div class="container mt-5 d-flex align-items-end gap-2">
            <img src="@TempData["Path1"];" style="@TempData["imageCss1"];" />
            <img src="@TempData["Path2"];" style="@TempData["imageCss2"];" />
            <img src="@TempData["Path3"];" style="@TempData["imageCss3"];" />
            <img src="@TempData["Path4"];" style="@TempData["imageCss4"];" />
            <img src="@TempData["Path5"];" style="@TempData["imageCss5"];" />

        </div>
        <hr />
        <h2>Task 2.2</h2>

        <div class="container mt-5 d-flex align-items-end" ">
            <img src="@TempData["Path1"];" style="@TempData["imageCss1mod"];" />
            <img src="@TempData["Path2"];" style="@TempData["imageCss2mod"];" />
            <img src="@TempData["Path3"];" style="@TempData["imageCss3mod"];" />

        </div>
    </div>
    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
